---
import { inputNumberStyle } from "./style.css";

interface Props {
  readonly name: string;
  readonly value: string;
}

const { name, value } = Astro.props;
---

<input type="number" name={name} value={value} class={inputNumberStyle} />
